---
sidebar_position: 2
---

# GetItem

### Store.get()

Create a get query.

```tsx
import { DynamoDBClient } from '@aws-cdk/client-dynamodb'
import { Store } from '@declanprice/dynostore'

const store = new Store('CustomerTable', new DynamoDBClient())

const getRequest = store.get<CustomerItem>();
```

### key(object)

Provide the item key to get.

```tsx
getRequest.key({id: '1' });
```

### consistent()

Require strongly consistent reads.

```tsx
getRequest.consistent();
```


### projection(string)

Project specific fields to be returned.

```tsx
getRequest.project('id,fistName,lastName');
```

### exec()

Execute the request.

```tsx
const customer = await getRequest.exec();
```

### tx()

Create a TransactGetItem.

```tsx
const customerTxItem = getRequest.tx();
```